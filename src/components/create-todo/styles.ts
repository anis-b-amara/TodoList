import { createStyles, makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() =>
  createStyles({
    paper: {
      width: "800px",
    },
    marginTop: {
      marginTop: "10px",
    }
  })
);
